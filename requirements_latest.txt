# SenseVoice 依赖包 - 最新版本配置
# 使用 PyTorch 2.3.0+ 和 NumPy 2.0+

modelscope>=1.9.0
funasr>=1.0.0
torch>=2.3.0  # 支持 NumPy 2.0+
torchaudio>=2.3.0
numpy>=2.0.0  # 使用最新 NumPy 2.x
scipy>=1.7.0
librosa>=0.9.0
soundfile>=0.10.0

# Web UI 相关（可选）
gradio>=3.0.0

# 其他工具
tqdm>=4.64.0

# 打包工具（可选，用于创建可执行文件）
pyinstaller>=5.0.0

# 注意：
# - 此配置使用最新的 PyTorch 和 NumPy
# - 需要测试所有功能是否正常
# - 如果遇到问题，可以使用 requirements.txt（稳定版配置）

