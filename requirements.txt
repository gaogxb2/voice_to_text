# SenseVoice 依赖包
modelscope>=1.9.0
funasr>=1.0.0
torch>=1.13.0
torchaudio>=0.13.0
numpy<2.0,>=1.21.0
scipy>=1.7.0
librosa>=0.9.0
soundfile>=0.10.0

# Web UI 相关（可选）
gradio>=3.0.0

# 其他工具
tqdm>=4.64.0

# 注意：numba 和 llvmlite 版本兼容性
# 如果遇到 llvmlite 编译错误，可以使用预编译版本：
# pip install llvmlite==0.45.1 --only-binary :all:
# pip install "numba<0.63"

