# 手动下载模型指南

如果自动下载失败，可以手动从 ModelScope 网站下载模型。

## 🌐 模型下载页面

### SenseVoiceSmall（推荐，约 900MB）

**网页地址：**
https://www.modelscope.cn/models/iic/SenseVoiceSmall

**直接下载链接：**
- 模型文件: https://www.modelscope.cn/models/iic/SenseVoiceSmall/files
- 或访问: https://modelscope.cn/models/iic/SenseVoiceSmall/summary

### SenseVoiceMedium（更大，性能更好）

**网页地址：**
https://www.modelscope.cn/models/iic/SenseVoiceMedium

## 📥 下载步骤

### 方法 1: 从网页下载

1. **访问模型页面**
   - 打开: https://www.modelscope.cn/models/iic/SenseVoiceSmall

2. **下载模型文件**
   - 点击页面上的"Files"标签
   - 找到 `model.pt` 文件（约 893MB）
   - 点击下载

3. **下载其他必要文件**
   - `config.yaml` - 配置文件
   - `tokens.json` - 词汇表
   - `chn_jpn_yue_eng_ko_spectok.bpe.model` - BPE 模型
   - `am.mvn` - 归一化文件
   - `configuration.json` - 配置信息

4. **创建目录结构**
   ```bash
   mkdir -p models/iic/SenseVoiceSmall
   ```

5. **放置文件**
   - 将所有下载的文件放到 `models/iic/SenseVoiceSmall/` 目录

### 方法 2: 使用 Git LFS 下载（如果可用）

```bash
# 安装 git-lfs（如果还没有）
git lfs install

# 克隆模型仓库
cd models
git clone https://www.modelscope.cn/models/iic/SenseVoiceSmall.git iic/SenseVoiceSmall
```

### 方法 3: 使用 wget/curl 下载（Linux/macOS）

```bash
# 创建目录
mkdir -p models/iic/SenseVoiceSmall
cd models/iic/SenseVoiceSmall

# 下载主要文件（需要登录后获取真实下载链接）
# 注意：以下链接可能需要替换为实际的下载地址
wget https://www.modelscope.cn/api/v1/models/iic/SenseVoiceSmall/repo?Revision=master&FilePath=model.pt
```

## 📁 完整的文件列表

下载后，`models/iic/SenseVoiceSmall/` 目录应包含：

```
models/iic/SenseVoiceSmall/
├── model.pt                              # 主模型文件（约 893MB，必需）
├── config.yaml                           # 配置文件（必需）
├── tokens.json                           # 词汇表（必需）
├── chn_jpn_yue_eng_ko_spectok.bpe.model # BPE 模型（必需）
├── am.mvn                                # 归一化文件（必需）
├── configuration.json                     # 配置信息（必需）
└── README.md                             # 说明文件（可选）
```

## ✅ 验证下载

下载完成后，运行以下命令验证：

```bash
python -c "
import os
model_dir = './models/iic/SenseVoiceSmall'
required_files = ['model.pt', 'config.yaml', 'tokens.json']
missing = [f for f in required_files if not os.path.exists(os.path.join(model_dir, f))]
if missing:
    print(f'缺少文件: {missing}')
else:
    print('✓ 所有必需文件已就绪')
"
```

## 🔗 快速访问链接

- **SenseVoiceSmall 模型页面**: https://www.modelscope.cn/models/iic/SenseVoiceSmall
- **SenseVoiceMedium 模型页面**: https://www.modelscope.cn/models/iic/SenseVoiceMedium
- **ModelScope 官网**: https://www.modelscope.cn

## 💡 提示

1. **登录账号**: 某些模型可能需要登录 ModelScope 账号才能下载
2. **浏览器下载**: 如果命令行下载失败，使用浏览器下载更可靠
3. **断点续传**: 浏览器下载支持断点续传
4. **文件完整性**: 下载后检查文件大小，确保完整

## 🚀 下载后使用

下载完成后，直接运行：

```bash
python webui.py
```

或

```bash
python example_usage.py --audio_path your_audio.wav --model_dir ./models/iic/SenseVoiceSmall
```

程序会自动检测并使用本地模型文件。


